# Coordinated release 0.3.1

Release `v0.3.1` was an unsuccessful Phase 6 publication attempt. Its tags and partial artifacts remain historical and are not reused or moved.

The corrected publication is superseded by `v0.3.2`, which retains the immutable Phase 5 Runtime at `v0.3.0` and publishes all Phase 6 Node components from new commits and tags.
